services:
  mongodb:
    build:
      context: .
      dockerfile: mongodb.Dockerfile
    container_name: omc_mongodb_container
    restart: always
    ports:
      - "27017:27017"
    environment:
      - MONGO_INITDB_DATABASE=omc_leads_db
    volumes:
      - omc_mongodb_data:/data/db

  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: omc_backend_container
    restart: always
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - MONGO_URI=mongodb://mongodb:27017/omc_leads_db
      - PORT=3000
    depends_on:
      - mongodb

  mongo-express:  # Interfaz gráfica para MongoDB
    image: mongo-express:latest
    container_name: omc_mongo_express_container
    restart: always
    ports:
      - "8081:8081"
    environment:
      - ME_CONFIG_MONGODB_SERVER=mongodb
      - ME_CONFIG_MONGODB_PORT=27017
      - ME_CONFIG_BASICAUTH_USERNAME=admin
      - ME_CONFIG_BASICAUTH_PASSWORD=pass
    depends_on:
      - mongodb

volumes:
  omc_mongodb_data:
    driver: local
